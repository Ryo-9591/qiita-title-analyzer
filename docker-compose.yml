services:
  qiita-analyzer:
    build: .
    container_name: qiita-title-analyzer
    volumes:
      # 生成されたファイルをホストにマウント
      - ./docs/images:/app/docs/images
    environment:
      - PYTHONUNBUFFERED=1
      - QIITA_TOKEN=${QIITA_TOKEN}
